# hy2
This is a one key shell script to install a hysteria2 server on linux. 

Just run the command below on your linux.
```
bash <(wget -qO- https://raw.githubusercontent.com/MaplinMa/hy2/main/deploy.sh 2> /dev/null)
```
